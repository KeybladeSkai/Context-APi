import { createContext } from "react";

// create a  store
// create a provider to feed the data
// so we create user context provider
const userContext=createContext();



export default userContext;